<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <import type="android.view.View" />

        <import type="com.dev.anhnd.android_mvvm_di_hilt.ui.notes.note_detail.NotesDetailViewModel" />

        <import type="com.dev.anhnd.android_mvvm_di_hilt.ui.main.MainViewModel" />

        <variable
            name="viewModel"
            type="NotesDetailViewModel" />

        <variable
            name="mainViewModel"
            type="MainViewModel" />

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="@dimen/_10sdp">

        <androidx.appcompat.widget.AppCompatTextView
            android:id="@+id/tvTitle"
            android:layout_width="match_parent"
            android:layout_height="@dimen/_50sdp"
            android:background="#CCF6CE"
            android:gravity="center"
            android:text="@{mainViewModel.note.title}"
            android:textSize="@dimen/_20ssp"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="Title" />

        <androidx.appcompat.widget.AppCompatTextView
            android:id="@+id/tvContent"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_marginTop="@dimen/_10sdp"
            android:background="#F4E9D7"
            android:gravity="top|start"
            android:padding="@dimen/_10sdp"
            android:text="@{mainViewModel.note.content}"
            android:textSize="@dimen/_14ssp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintTop_toBottomOf="@id/tvTitle"
            tools:text="Content" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
