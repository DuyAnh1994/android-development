<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <import type="android.view.View" />

        <import type="androidx.recyclerview.widget.RecyclerView.Adapter" />

        <import type="com.dev.anhnd.android_mvvm_di.ui.notes.list.ListNotesViewModel" />

        <import type="android.view.View.OnClickListener" />

        <import type="com.dev.anhnd.android_mvvm_di.ui.main.MainViewModel" />

        <variable
            name="viewListener"
            type="OnClickListener" />

        <variable
            name="adapter"
            type="Adapter" />

        <variable
            name="viewModel"
            type="ListNotesViewModel" />

        <variable
            name="mainViewModel"
            type="MainViewModel" />

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rvNotes"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
            app:rv_set_adapter="@{adapter}"
            app:rv_set_fix_size="@{true}"
            tools:listitem="@layout/item_notes" />

        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/btnAddNotes"
            deBounceClick="@{viewListener::onClick}"
            android:layout_width="@dimen/_50sdp"
            android:layout_height="@dimen/_50sdp"
            android:layout_margin="@dimen/_20sdp"
            android:gravity="center"
            android:src="@drawable/ic_add_notes"
            app:fabCustomSize="@dimen/_50sdp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
